{
  "name": "@sharpwasm/monorepo",
  "packageManager": "yarn@3.2.4",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "start:sample": "lerna run start",
    "compiler": "lerna run watch --scope=@editorsharp/compiler"
  },
  "workspaces": [
    "packages/*",
    "packages/wasm/src/EditorSharp.Compiler",
    "samples/*"
  ],
  "devDependencies": {
    "lerna": "^6.0.1",
    "prettier": "^2.7.1"
  }
}
